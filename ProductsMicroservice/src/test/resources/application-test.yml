spring:
  kafka:
    consumer:
      group-id: product-created-events
      properties:
        spring:
          json:
            trusted:
              packages: "com.joaocarlos.core.events"
      auto-offset-reset: earliest
product:
  created:
    events:
      topic:
        name: product-created-events-topic